import React from 'react';
import styled from 'styled-components';

interface ParamsTabProps {
  // Add props if needed
}

const ParamsTab: React.FC<ParamsTabProps> = (props) => {
  return (
    <div>
      <p>This is the Params tab</p>
    </div>
  );
};

export default ParamsTab;
