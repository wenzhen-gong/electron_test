import React from 'react';
import styled from 'styled-components';

interface BodyTabProps {
  // Add props if needed
}

const BodyTab: React.FC<BodyTabProps> = (props) => {
  return (
    <div>
      <p>This is the Body tab</p>
    </div>
  );
};

export default BodyTab;
