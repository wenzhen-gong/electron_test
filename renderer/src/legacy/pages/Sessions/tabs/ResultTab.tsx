import React from 'react';
import styled from 'styled-components';

interface ResultTabProps {
  // Add props if needed
}

const ResultsTab: React.FC<ResultsTabProps> = (props) => {
  return (
    <div>
      <p>This is the Results tab</p>
    </div>
  );
};

export default ResultsTab;
