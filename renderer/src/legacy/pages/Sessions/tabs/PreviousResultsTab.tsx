import React from 'react';
import styled from 'styled-components';

interface PreviousResultsTabProps {
  // Add props if needed
}

const PreviousResultsTab: React.FC<PreviousResultsTabProps> = (props) => {
  return (
    <div>
      <p>This is the Previous Results tab</p>
    </div>
  );
};

export default PreviousResultsTab;
