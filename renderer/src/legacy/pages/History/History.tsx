import React from 'react';

interface HistoryProps {
  // Add props if needed
}

const History: React.FC<HistoryProps> = (props) => {
  return (
    <div>
      <p>Hello World History!</p>
    </div>
  );
};

export default History;
